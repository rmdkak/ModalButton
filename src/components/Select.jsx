import React from "react";

function Select() {
  return (
    <div>
      <h1>Select</h1>
      <div>
        <select>리액트</select>
        <select>리액트</select>
      </div>
    </div>
  );
}

export default Select;
